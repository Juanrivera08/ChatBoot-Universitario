# ============================================================
# VARIABLES DE ENTORNO — CHATBOT USH
# Copia este archivo a .env y llena los valores reales
# ============================================================

# Servidor
NODE_ENV=development
PORT=3001
LOG_LEVEL=info

# CORS
ALLOWED_ORIGINS=http://localhost:5173

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ush_chatbot
DB_USER=postgres
DB_PASSWORD=postgres123

# ChromaDB
CHROMA_URL=http://localhost:8000

# Google Gemini (GRATIS) — obtener en: https://aistudio.google.com/app/apikey
GOOGLE_API_KEY=AIza...tu_clave_aqui...

# JWT — cambia esto por cualquier texto largo y secreto
JWT_SECRET=ush_chatbot_secreto_super_largo_2024_cambiame
JWT_EXPIRES_IN=8h

# RAG
CHUNK_SIZE=500
CHUNK_OVERLAP=50
TOP_K=5
